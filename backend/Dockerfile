# Use a super small Python version
FROM python:3.12-slim

# Set the work directory
WORKDIR /app

# No extra libraries needed for basic missions, keeps it fast!
# This container will be created and destroyed in seconds.
CMD ["python", "solution.py"]